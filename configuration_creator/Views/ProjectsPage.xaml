<Page
    x:Class="configuration_creator.Views.ProjectsPage"
    Style="{DynamicResource MahApps.Styles.Page}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:local="clr-namespace:configuration_creator.Converters"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:vm="clr-namespace:configuration_creator.ViewModels"
    xmlns:properties="clr-namespace:configuration_creator.Properties"
    xmlns:behaviors="clr-namespace:configuration_creator.Behaviors"
    mc:Ignorable="d"
    d:DesignHeight="600" d:DesignWidth="800">

    <Page.Resources>
        <local:DictionaryValueConverter x:Key="DictionaryValueConverter"/>
        <DataTemplate x:Key="contentGridItemTemplate">
            <Grid
                Width="200"
                Height="200"
                Background="{DynamicResource MahApps.Brushes.Gray10}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="12">
                    <TextBlock
                        FontSize="22"
                        Text="{Binding Client}"
                        HorizontalAlignment="Center"
                        FontWeight="SemiBold"
                        TextWrapping="Wrap"
                        TextAlignment="Center"
                        MaxWidth="180"/>
                    <TextBlock
                        Margin="0,10,0,0"
                        HorizontalAlignment="Center"
                        Style="{StaticResource BodyTextStyle}"
                        FontSize="16"
                        Text="{Binding CC}"
                        TextWrapping="Wrap"
                        TextAlignment="Center"
                        MaxWidth="180"/>
                    <TextBlock
                        Margin="0,30,0,0"
                        HorizontalAlignment="Center"
                        Style="{StaticResource BodyTextStyle}"
                        FontSize="14"
                        Text="{Binding ProjectInfo}"
                        TextWrapping="Wrap"
                        TextAlignment="Center"
                        MaxWidth="180"/>
                </StackPanel>
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock
            Style="{StaticResource PageTitleStyle}"
            Margin="{StaticResource MediumLeftMargin}"
            Text="{x:Static properties:Resources.ProjectsPageTitle}" />

        <!-- Main content split into two columns -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Left: Project tiles -->
            <ListView
                Grid.Column="0"
                Margin="{StaticResource SmallLeftMargin}"
                SelectionMode="Single"
                ItemsSource="{Binding Source}"
                SelectedItem="{Binding SelectedTile, Mode=TwoWay}"
                Style="{StaticResource ContentGridListViewStyle}"
                ItemTemplate="{StaticResource contentGridItemTemplate}">
            </ListView>

            <!-- Right: Selected project details -->
            <ScrollViewer Grid.Column="1" Margin="24,0,0,0" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="Project Details" FontWeight="Bold" FontSize="18" Margin="0,0,0,16"/>
                    <!--
                        The ItemsControl below will generate a row for each column in the Excel file.
                        The DataContext is the SelectedTile, and the Columns property is a list of column names.
                    -->
                    <ItemsControl ItemsSource="{Binding Columns}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <!-- Label: Column name -->
                                    <TextBlock Text="{Binding}" Width="150" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                    <!-- TextBox: Value for this column in the selected row -->
                                    <TextBox
                                        Width="200"
                                        Margin="8,0,0,0">
                                        <TextBox.Text>
                                            <MultiBinding Converter="{StaticResource DictionaryValueConverter}" Mode="OneWay">
                                                <Binding Path="DataContext.SelectedRowCells" RelativeSource="{RelativeSource AncestorType=Page}" />
                                                <Binding />
                                            </MultiBinding>
                                        </TextBox.Text>
                                    </TextBox>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
